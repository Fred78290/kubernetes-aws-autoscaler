{
    "apiVersion": "apps/v1",
    "kind": "Deployment",
    "metadata": {
        "name": "aws-autoscaler",
        "namespace": "kube-system",
        "labels": {
            "k8s-app": "aws-autoscaler"
        }
    },
    "spec": {
        "replicas": 1,
        "selector": {
            "matchLabels": {
                "k8s-app": "aws-autoscaler"
            }
        },
        "template": {
            "metadata": {
                "labels": {
                    "k8s-app": "aws-autoscaler"
                },
                "annotations": {
                    "scheduler.alpha.kubernetes.io/critical-pod": ""
                }
            },
            "spec": {
                "nodeSelector": {
                    "master": "true"
                },
                "containers": [
                    {
                        "image": "$REGISTRY/aws-autoscaler:v1.21.0-dev",
                        "name": "aws-autoscaler",
                        "resources": {
                            "limits": {
                                "cpu": "100m",
                                "memory": "300Mi"
                            },
                            "requests": {
                                "cpu": "100m",
                                "memory": "300Mi"
                            }
                        },
                        "command": [
                            "/usr/local/bin/aws-autoscaler",
                            "--config=/etc/cluster/kubernetes-aws-autoscaler.json",
                            "--save=/etc/cluster/aws-autoscaler-state.json",
                            "--log-level=info"
                        ],
                        "imagePullPolicy": "Always",
                        "volumeMounts": [
                            {
                                "name": "aws-autoscaler",
                                "mountPath": "/etc/cluster"
                            }
                        ]
                    }
                ],
                "volumes": [
                    {
                        "name": "aws-autoscaler",
                        "hostPath": {
                            "path": "/etc/cluster"
                        }
                    }
                ]
            }
        }
    }
}